

const primary = Object.freeze({
    base: '#329988',
});
const green = Object.freeze({
    base: '#BFDC36',

});
const propurple = Object.freeze({
    base: '#7562A1',

});

export default Object.freeze({
    primary,
    green,
    propurple
});

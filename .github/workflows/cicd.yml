name: CI/CD

on:
  pull_request:
  push:
    branches:
      - "+([0-9])?(.{+([0-9]),x}).x"
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/cache@v2
        with:
          path: .cache/
          key: ${{ hashFiles('package-lock.json') }}

      - run: echo -e ".git\n.cache" > .dockerignore
      - uses: docker/login-action@v2
        with:
          registry: ghcr.io
          # username: ${{ github.actor }}
          username: ${{ secrets.َGHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}
      - uses: docker/setup-buildx-action@v2
        with:
          driver-opts: network=host
      - uses: docker/build-push-action@v3
        with:
          file: .github/Dockerfile
          cache-from: type=local,src=.cache
          cache-to: type=local,dest=.cache,mode=max
          target: cache
          push: false
          context: .
      - uses: docker/build-push-action@v3
        with:
          file: .github/Dockerfile
          tags: |
            ghcr.io/${{ github.repository }}:${{ github.ref_type == 'tag' && github.ref_name || github.sha }}
            ghcr.io/${{ github.repository }}:latest
          cache-from: type=local,src=.cache
          push: true
          context: .

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - run: echo Test

  # release:
  #     needs: test
  #     runs-on: ubuntu-latest
  #     if: github.event_name == 'push'
  #     steps:
  #         - uses: actions/checkout@v3
  #         - uses: actions/setup-node@v3
  #           with:
  #               node-version: 17

  #         - run: cp .github/.releaserc.yml .
  #         - run: curl -L https://github.com/regclient/regclient/releases/latest/download/regctl-linux-amd64 > regctl && chmod 755 regctl
  #         - run: npm i -g semantic-release @semantic-release/changelog @semantic-release/exec @semantic-release/git
  #         - run: semantic-release
  #           env:
  #               GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #               CI_REGISTRY: ghcr.io
  #               CI_REGISTRY_USER: ${{ github.actor }}
  #               CI_REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
  #               CI_REGISTRY_IMAGE: ghcr.io/${{ github.repository }}
  #               CI_COMMIT_SHA: ${{ github.sha }}
